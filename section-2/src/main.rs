/*
- cargo build: build and save compiled file.
- cargo run: run file, faster than build.
- cargo check: just for check if file can be compiled.
- cargo build --release: faster compiled code ready for release (takes more time).
- cargo fmt: format code, using --all entire files will format. 
*/

fn main() {
    println!("hello, world using cargo");
}
